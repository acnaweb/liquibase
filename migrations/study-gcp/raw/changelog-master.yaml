databaseChangeLog:
  - property:
      name: projectId
      value: ${projectId}
  - property:
      name: dataset
      value: ${dataset}

  - includeAll:
      path: changes
      relativeToChangelogFile: true
